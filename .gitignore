# extensions
*.pyc
*.png
*.jpg
*.tar.gz
*.tar.xz
*.tar
*.zip
*.gif
*.rar
LICENSE

# cache
__pycache__/
.ipynb*/

# logs
logs/
quant_logs*/
qual_logs*/
saved_ckpt*/
logs_home/
logs_building/

# data
data
demo_data/

# bak
bak*/

# archived cleanup (moved, reversible — see legacy/archive_*/RESTORE.md)
legacy/

Logs_GT
Saved_Checkpoint/
eval_results/
pytorch3d/
#ml/training junk
__pycache__/
*.pyc
*.pyo
*.pt
*.pth
*.ckpt
wandb/
runs/
logs/
checkpoints/
# NOTE: `datasets/` here is the dataset *loader code* and must stay tracked; actual
# data lives in data/ (ignored above). A bare `datasets/` rule here used to shadow
# the code — removed 2026-07-08.

# Visual Studio / build junk
.vs/
build/
bin/
obj/
x64/
x86/
Debug/
Release/
sdfusion/

# Python virtual environments
sdfusion/
venv/
env/
.venv/
*.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# #127 height-map generator: a 165 MB derived cache of the corpus as height fields, and its
# checkpoints. Both are rebuilt by `train_height_map_generator.py` from data/ and are not evidence.
outputs/height_map_generator/
